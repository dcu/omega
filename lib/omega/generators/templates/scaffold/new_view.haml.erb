#<%= underscore_name %>-new
  %h1
    New <%= class_name %>

  %form(ng-submit="save()")
    <%- fields.each do |name, type| %>
    <%- next if name =~ /(^_)|(_id$)/ %>
    %label(for="<%= name %>")
      <%= name.titleize %>
    %input(type="text" name="<%= name %>" ng-model="<%= resource_name %>.<%= name %>")
    <%- end %>
    %button(type="submit")
      Create <%= resource_name %>

